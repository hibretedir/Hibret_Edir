# Environment variables - NEVER commit these
.env
.env.local
.env.production
.env.development

# Python
__pycache__/
*.pyc

# Node modules (for future backend)
node_modules/
node_modules.trash-*/
.hibret-temp-node-modules
node_modules.gdrive-backup/
npm-debug.log*

# OS files
.DS_Store
Thumbs.db

# IDE files
.vscode/
.idea/

# Build outputs
dist/
build/

# Netlify
.netlify/
netlify/functions/.dev-pins.json

# Local seed data (member PII, invoices) — NEVER commit
# Only example files and .gitkeep are tracked.
data/*
!data/.gitkeep
!data/*.example.json
# includes: members xlsx, waiting list xlsx, PayPal csv, unpaid_invoices.*, any future exports
public/portal/members-snapshot.json

folders.tsv
